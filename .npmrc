# Supply chain security, only modify if you have a good reason

# Prevent installing releases younger than 7 days
min-release-age=7

# Block postinall/preinstall scripts to prevent malicious code execution
ignore-scripts=true

# Hard-fail if Node/npm versions don't match package.json engines
engine-strict=true
